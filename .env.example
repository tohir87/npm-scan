# Copy to .env and fill in. .env is gitignored; this file is not.

# Free Auth-Key from https://auth.abuse.ch/ — enables URL reputation lookups.
# Without it npm-scan still runs, but skips the URLhaus source.
URLHAUS_AUTH_KEY=
